# خطة تنفيذ التعديلات (وضع المالك + مهلة إعادة الاتصال + تصغير الواجهة)

## نظرة عامة على البنية الحالية
- ملف واحد: `كود نيمز.html`
- الشاشات: `nameScreen` ← `roomsScreen` ← `rolesScreen` ← `gameScreen`
- Firebase: `/rooms/{roomId}` فيه `name, maxPlayers, mode, customWords, globalChat, game{...}`
- `game` فيه: `players, roles, board, state, clue, winner, countdown, chats, cancel`
- المراقبة تعتمد على `PLAYERS_REF.on('value')` و `checkPlayersIntegrity()`

---

## 1) دخول الملّاك (كلمة السر SMO)
**المكان:** `nameScreen` + دوال جديدة
- زر «دخول الملاك» في `nameScreen`.
- نافذة إدخال كلمة سر → مقارنة مع `SMO`.
- متغير عام `isSiteOwner = false`.
- عند النجاح: `isSiteOwner = true` + إظهار حالة «وضع المالك» + عدم إلزامه بالأفتار.
- المالك يحتاج اسماً فقط (بدون أفتار) للدخول.

## 2) المالك مخفي ولا يُحسب ضمن العدد
**المبدأ الأساسي:** لا يُكتب المالك داخل `game/players` إطلاقاً.
- في `attemptJoinRoom`: إذا `isSiteOwner` → لا ننشئ `PLAYERS_REF.push()`، و`myPlayerId = null`.
- بدلاً من ذلك يُسجَّل في عقدة منفصلة `game/spectators/{id}` (لا يقرأها أي منطق للعدد).
- تجاوز فحص «الغرفة ممتلئة».
- الحماية المهمة: كل الدوال التي تعتمد على `myPlayerId` يجب ألا تُفعَّل للمالك:
  - `checkPlayersIntegrity()` → تخرج فوراً إذا `isSiteOwner` (حتى لا يحذف الغرفة).
  - `selectRole()` / `leaveRole()` / `randomizeTeams()` / `surrenderGame()` → ممنوعة.
  - `confirmGuess()` / `toggleDoubt()` / `submitClue()` / `endGuesserTurnManual()` → ممنوعة.
  - `checkIfLobbyFull()` / `startCountdown()` / `startCoreGame()` → لا يبدأ اللعبة.
  - `resetGame()` → لا يكتب في قاعدة البيانات.
  - في `attachGameListeners`: شرط الطرد `!gameState.players[myPlayerId]` يجب تخطيه للمالك.
- ملاحظة: `cleanupOrphanRoles` تكتب في `ROLES_REF` — نمنعها للمالك.

## 3) المالك يشاهد كل شيء
- يرى `rolesScreen` و`gameScreen` حسب `state.gameStarted`.
- يرى اللوحة بألوانها الحقيقية (مثل الكاتب) → `writer-mode` للمالك.
- يرى الشات العام. الشات الخاص: يمكنه القراءة (اختياري) — سنُبقيه على الشات العام فقط لتفادي التعقيد، لكن نعرض له كل شيء يجري في اللعبة.
- إخفاء أزرار اللعب عنه: التخلي عن المنصب، تيمات عشوائية، الانسحاب، إنهاء الدور، إدخال التلميح.

## 4) وسم «أحد الملاك» في الشات
- عند الإرسال: `isOwnerMsg: true` داخل رسالة الشات.
- في `renderChatMessage`: إظهار شارة ذهبية «أحد الملاك» بجانب الاسم.

## 5) زر حذف الغرفة (أحمر) في بطاقة كل غرفة
- في `renderRoomsList`: إذا `isSiteOwner` → زر «حذف» أحمر.
- `deleteRoomCompletely(rId)`: تأكيد ثم `ROOMS_REF.child(rId).remove()` (يحذف الغرفة وكل ما بداخلها: اللاعبين واللوحة والشات).
- `event.stopPropagation()` حتى لا يدخل الغرفة عند الضغط على الحذف.
- إذا كان أحد داخل الغرفة المحذوفة → مستمعوه سيجدون البيانات فارغة → نضيف حماية تُخرجهم للوبي برسالة.

## 6) زر خروج للمالك من داخل الغرفة
- موجود أصلاً زر «خروج» في `rolesScreen`؛ نضيف واحداً في `gameScreen` يظهر للمالك فقط.
- `exitRoom()` يجب أن تتعامل مع المالك: لا `broadcastCancel`، لا حذف لاعبين، فقط إزالة المشاهد والعودة للوبي.
  (مهم جداً: خروج المالك يجب ألا يلغي جولة اللاعبين.)

## 7) زر «كشف الفوزات» في قائمة الغرف
- عقدة جديدة عامة: `/winsHistory` (خارج الغرف) — تبقى بعد حذف الغرف.
- تُكتب عند الفوز في `showVictory`/`checkWinLocal`: أسماء الفريق الفائز والخاسر + الوقت + اسم الغرفة.
- **من يكتب؟** لتفادي التكرار: يكتبها المالك المنطقي للغرفة (`currentPlayer.isOwner`) فقط.
- الزر يظهر للمالك فقط في `roomsScreen` → نافذة تعرض آخر 30 نتيجة:
  «فلان و فلان ← انتصروا على ← فلان و فلان».

## 8) مهلة 30 ثانية عند انقطاع الاتصال (الأهم)
**السلوك الحالي:** أي نقص لاعب → `cancelGame` فوراً.
**السلوك الجديد:**
- عند اكتشاف نقص لاعب أثناء اللعب: بدل الإلغاء، نبدأ «فترة انتظار».
- شريط صغير أعلى الشاشة: «(الاسم) غادر — سيتم إلغاء الجولة خلال N ثانية إن لم يعد» مع عداد تنازلي.
- نحتاج معرفة **اسم** اللاعب المفقود: من `gameState.roles` نعرف أي دور فرغ، ونحتاج نسخة سابقة من أسماء اللاعبين → نحتفظ بـ `lastKnownPlayers` (لقطة قبل الحذف).
- التخزين في قاعدة البيانات حتى يتزامن الجميع: `game/pendingDisconnect = { name, roleId, deadline }`.
  - يكتبها أول من يكتشف (transaction لتفادي التكرار).
- **العودة:** عندما يعود اللاعب ويأخذ نفس الدور → نمسح `pendingDisconnect` ويختفي الشريط.
  - كيف يعود لنفس الدور؟ `selectRole` عادية. لكن الأفضل: عند دخول لاعب باسم مطابق للاسم المفقود نعيد ربطه تلقائياً بالدور.
  - **القرار:** إعادة الربط التلقائي بالاسم — لأن المستخدم قال «لو رجع للموقع وأعاد اتصاله». عند `attemptJoinRoom`، إذا وُجد `pendingDisconnect` واسم الداخل مطابق → يُسند له نفس الدور وتُمسح العقدة وتستمر اللعبة.
- **انتهاء المهلة:** أول عميل يرى انتهاء الوقت (والأفضل: صاحب `isOwner`) يستدعي `cancelGame` بالرسالة المعتادة.
- يجب إيقاف العداد إذا انتهت اللعبة أو أُلغيت.
- المؤقّت المحلي يُنظَّف في `clearPendingTimers`/مغادرة الغرفة.

## 9) تصغير واجهة شاشة الأدوار
CSS فقط (لا يمس المنطق):
- `.role-box`: 100px → 74px، الصورة 50 → 38، الخط 14 → 12
- `.role-title`: 17 → 13
- `.team`: padding 25 → 14، gap 12 → 8
- `.teams-container`: gap 25 → 14
- `.player-avatar`: 65 → 46، `.player-name-label`: 15 → 12
- `.players-lobby`: padding 20 → 12، gap 20 → 12
- `.crown-icon`: 26 → 18
- `.leave-role-btn` / `.random-teams-btn` / `.icon-btn` / `.chat-toggle-btn` / `.settings-btn-text`: تصغير padding والخط
- العناوين `المتواجدين` (24 → 17) و`اختر فريقك ودورك` (افتراضي → 18)

---

## ترتيب التنفيذ الآمن
1. CSS التصغير (منعزل تماماً).
2. متغيرات المالك + شاشة كلمة السر.
3. منع المالك من كل مسارات الكتابة (الحماية الدفاعية).
4. دخول المالك كمشاهد + إخفاء أزرار اللعب.
5. شارة «أحد الملاك» في الشات.
6. زر حذف الغرفة + حماية من فيها.
7. زر كشف الفوزات + تسجيل النتائج.
8. مهلة 30 ثانية (الأعقد) — آخر شيء.
9. اختبار شامل لكل ما سبق + التأكد أن الإصلاحات السابقة لم تنكسر.

## نقاط خطر يجب الانتباه لها
- `checkPlayersIntegrity` تحذف الغرفة إذا `players` فاضية — المالك المشاهد يجب ألا يشغّلها.
- `attachGameListeners` تطرد من ليس في `players` — المالك ليس فيها أصلاً.
- `myPlayerId = null` للمالك → أي `PLAYERS_REF.child(null)` سيرمي خطأ. لا بد من حراسة كل استخدام.
- `onDisconnect` للمشاهد يجب أن يزيل عقدة المشاهد فقط.
- عند حذف غرفة يجلس فيها لاعبون، يجب ألا يُفهم ذلك كـ«انقطاع» فيُظهر رسالة خاطئة.

---

# الجولة الخامسة: الأمان والتجربة والمظهر

## ١) إغلاق ثغرة XSS (الأخطر)
كانت نصوص المستخدمين تُحقن مباشرة عبر `innerHTML`، فأي لاعب يقدر ينفّذ كوداً
على أجهزة كل من في الغرفة، والهجمة تتكرر لأن الشات مخزّن في قاعدة البيانات.

- `escapeHtml()` تهرّب `& < > " '`.
- `escapeAttr()` للقيم داخل سمات `onclick`.
- `safeImgSrc()` تقبل `data:image/*` و`https` فقط، وترفض `javascript:`
  و`data:text/html` وأي محاولة كسر للسمة.
- طُبّقت على **كل** نقاط الحقن: رسائل الشات (النص والاسم والأفتار)،
  أسماء اللاعبين في اللوبي ومربعات الأدوار ولوحات الفرق، أسماء الغرف،
  كلمات اللوحة، أفتار الكاشف، وسجل الفوزات.

## ٢) كلمة سر الملاك
- لم تعد مكتوبة صراحة؛ يُخزَّن **هاش SHA-256 مع ملح** ويُقارن عبر `crypto.subtle`.
- قفل 30 ثانية بعد 5 محاولات خاطئة.
- ملاحظة: هذا يرفع الصعوبة فقط. الحماية الحقيقية تحتاج Firebase Auth + قواعد أمان.

## ٣) حدود الإدخال
الرسالة 300 حرف، الاسم 20، التلميح 40، اسم الغرفة 30، الكلمات المخصصة 200 كلمة.
مطبّقة في الكود وفي `maxlength` معاً.

## ٤) التجاوب مع الجوال
خمس `@media`: 900px، 600px، 380px، الوضع الأفقي المنخفض، وتقليل الحركة.
على الجوال: الفريقان فوق بعض، البطاقات أكبر نسبةً، الشات يتوسّع، النوافذ تتقلص.
أُزيل `user-scalable=no` (كان يمنع التكبير — مشكلة وصولية).

## ٥) حفظ الاسم والأفتار
`localStorage` (`cn_name`, `cn_avatar`) مع تمرير الأفتار المسترجع على `safeImgSrc`.
تصغير الأفتار 150→96 بكسل وجودة 0.85→0.70 (توفير ~65% من حجم البيانات المنقولة).

## ٦) مؤقت الدور (90 ثانية)
- يظهر عدّاد؛ آخر 10 ثوانٍ باللون الأحمر مع نبض.
- عند الانتهاء ينتقل الدور تلقائياً بدل تجميد اللعبة.
- **من ينفّذ الانتقال:** صاحب الدور فوراً، ومالك الغرفة احتياطاً بعد 3 ثوانٍ
  إن كان صاحب الدور متجمداً — مع حارس `turnTimerFiredFor` يمنع التنفيذ المزدوج.
- **يتوقف فوراً** أثناء انتظار عودة لاعب منقطع، ويستأنف عند رجوعه،
  ويتوقف عند الفوز أو الإلغاء.

## ٧) سجل الجولة
محلي بالكامل (لا يلمس قاعدة البيانات) — يسجّل التلميحات والتخمينات
وانتهاء الوقت، بحد 60 حدثاً، ويُصفَّر مع كل جولة جديدة. نصوصه مهرَّبة.

## ٨) الأصوات
مولّدة داخلياً بـWebAudio (بدون أي ملفات = بدون تحميل إضافي):
دورك، رسالة واردة، تخمين صحيح/خاطئ، فوز، تكّة العد التنازلي.
زر كتم يُحفظ اختياره، ولا يعمل الصوت قبل أول لمسة (سياسة المتصفحات).

## ٩) تحسينات المظهر
حركات تعتمد `transform`/`opacity` فقط (مسرّعة بالعتاد):
ظهور متدرج للبطاقات، لمعة على بطاقة الغرفة، انقلاب خفيف عند كشف الكلمة،
ضغطة محسوسة للأزرار، شريط تمرير أنيق، واحترام `prefers-reduced-motion`.

## الاختبار
108 حالة ناجحة: 26 أمان (تشمل حقن فعلي عبر الشات والاسم والغرفة والأفتار)،
40 للميزات الجديدة، 42 انحدار لجولات 1-4.


---

# الجولة السادسة: التراجع حسب طلب المستخدم

طلب المستخدم إبقاء بعض تحسينات الجولة الخامسة وإرجاع الباقي.

## المُبقى ✅
- **سد ثغرة XSS** كاملاً: `escapeHtml` و`escapeAttr` و`safeImgSrc` على كل نقاط الحقن.
- **كلمة سر الملاك**: هاش SHA-256 مع ملح + قفل 30 ثانية بعد 5 محاولات.
- **حدود الإدخال** (رسالة 300، اسم 20، تلميح 40، اسم غرفة 30).
- **حفظ الاسم والأفتار** في `localStorage` مع تمرير الأفتار على `safeImgSrc`.
- **مؤقت الدور** 90 ثانية بكل منطقه (التوقف عند انقطاع لاعب والاستئناف عند عودته).
- **سجل الجولة** المحلي.
- **الأصوات** المولّدة بـWebAudio وزر الكتم.

## المُزال ❌
- كل كتلة «تحسينات المظهر والتأثيرات»: `fadeUp`، `shimmer`، `softPulse`،
  لمعة بطاقة الغرفة، انقلاب كشف الكلمة، ضغطة الأزرار، شريط التمرير العام،
  `tap-highlight`، و`prefers-reduced-motion`.
- **كل استعلامات `@media` الخمسة** (التجاوب مع الجوال).
- **تغييرات الأحجام**: رجع الأفتار إلى 150 بكسل وجودة 0.85،
  ورجع `viewport` إلى `maximum-scale=1.0, user-scalable=no`.

ملاحظة: تصغير واجهة شاشة الأدوار من **الجولة الرابعة** بقي كما هو،
لأنه كان طلباً صريحاً سابقاً وليس ضمن تعديلات الجولة الخامسة.

بعد الإزالة: الحركات في الملف = 9 فقط، وهي **مطابقة تماماً** لحركات الكود
قبل الجولة الخامسة (تحقق آلي بالمقارنة مع الكوميت 53782d2).

## الاختبار
55 حالة ناجحة: التحقق من اختفاء كل ما طُلب إزالته، وسلامة كل ما طُلب إبقاؤه،
وانحدار كامل لجولات 1-4 (الفوز، مهلة 30 ثانية، الشات العام، وضع المالك).

---

# إصلاح: الشاشة المختلطة عند عودة اللاعب المنقطع

## البلاغ
لاعب ينقطع أثناء الجولة ويظهر عداد الثلاثين ثانية. يفتح الموقع من جديد
ويدخل نفس الغرفة، فتظهر له **شاشة مختلطة** بين اختيار الأدوار واللعب،
ولا يستطيع اللعب ولا تحريك الصفحة لا فوق ولا تحت.

## السبب الجذري
في `attemptJoinRoom` كان الترتيب:

```
attachGameListeners();                 // مستمع STATE_REF يرى gameStarted=true
                                       // فيُظهر gameScreen ويخفي rolesScreen
rolesScreen.style.display = 'block';   // ← السطر التالي يُظهرها مرة أخرى بلا شرط!
```

فتبقى الشاشتان ظاهرتين معاً. ولأن `#gameScreen` مُعرّف بـ`height: 100vh`
وأزراره `position: absolute`، تتراكم شاشة الأدوار تحته ويصبح التمرير معطّلاً —
وهذا مطابق تماماً لما ظهر في الصور.

## الإصلاح
1. لا تُظهر شاشة الأدوار إلا إذا لم يكن `gameScreen` ظاهراً بالفعل
   (طُبّق على مسار اللاعب العادي **ومسار المالك** — كان فيه نفس العيب،
   وهو ما يفسّر خلل دخول المالك أثناء اللعب).
2. عند بداية `attemptJoinRoom` تُخفى شاشة اللعب ويُزال `game-active`
   حتى لا تبقى بقايا شاشة من غرفة سابقة.

## الاختبار
74 حالة ناجحة:
- 32 حالة مخصصة لصحة الشاشة المعروضة، قاعدتها «شاشة واحدة فقط ظاهرة»
  في 9 سيناريوهات (العائد بعد انقطاع، داخل جديد أثناء اللعب، غرفة فاضية،
  الانتقال بين الغرف، المالك أثناء اللعب وبدونها، نهاية الجولة،
  الإلغاء بالمهلة، وجولة جديدة بعد عودة).
- 42 حالة انحدار لكل ما سبق (الأمان، كلمة السر، الملف الشخصي، المؤقت،
  السجل، الأصوات، مهلة 30 ثانية، الشات، وضع المالك، حذف الغرف).

## الجولة الثامنة: وضوح الأفتار + حركة المربع + زر التأكيد

### ١) الأفتار المغبش
**السبب:** `previewAvatar()` كان يصغّر الصورة إلى 150×150 بكسل ويحفظها بجودة JPEG 0.85،
بينما دائرة الأفتار مقاسها 130px في CSS. على شاشات الجوال بكثافة 2x–3x تحتاج الدائرة
260–390 بكسل فعلية، فتُمدد صورة الـ150 بكسل → تغبيش في كل الصور.

**الحل:** رفع الحد إلى 400×400 بكسل، ورفع الجودة إلى 0.92، وتفعيل التنعيم عالي الجودة
(`imageSmoothingQuality = 'high'`). لم تتغير أي مقاسات عرض — فقط دقة الصورة المحفوظة.

### ٢) إرجاع حركة المربع الانسيابية
- `@keyframes cardRevealEpic` صارت تحترم `--card-scale` (كانت تتجاهله وترجع المربع لحجم خاطئ).
- منحنى انسيابي مرن: `cubic-bezier(0.34, 1.4, 0.64, 1)` بمدة 0.55s.
- ضغطة الزر الأصفر نفسه: `.confirm-btn:active { transform: scale(0.88) }` مع انتقال ناعم.
- باقي التأثيرات المحذوفة في الجولة السادسة بقيت محذوفة (عدد الحركات ما زال 9).

### ٣) الزر الأصفر يغطي الكلمة
**السبب:** الزر 30×30 بكسل عند `top/right: 5px` فوق مربع ارتفاعه ~55px في الجوالات الصغيرة،
والنص متمركز رأسياً وأفقياً فيقع تحته.

**الحل (حمايتان مستقلتان):**
- تصغير الزر إلى 22×22 وتقريبه للحافة (`top/right: 2px`).
- صنف `has-confirm` يضيف `padding-top: 16px` للمربع و`padding-right: 18px` للنص،
  فينزاح النص رأسياً وأفقياً بعيداً عن الزر — ويُطبَّق فقط عند ظهور الزر.
- التفاف الكلمات الطويلة (`overflow-wrap: anywhere`) بدل اختفائها.

**توحيد المنطق:** أُضيفت `setConfirmVisible(btn, show)` و`setAllConfirms(show, onlyUnrevealed)`
لتزامن ظهور الزر مع صنف التباعد في المواضع الستة كلها بدل تكرار `style.display` يدوياً.

**التحقق الحسابي** (لتعذّر تشغيل متصفح): الفجوة بين أسفل الزر وأعلى النص موجبة على
360×640 و390×844 و430×932 و768×1024.

**الاختبارات:** 28/28 لواجهة الجولة الثامنة + 28/28 اختبار انحدار = 56/56.

## الجولة التاسعة: تقطيع الكلمات + الإعدادات الافتراضية + الصوت + التأثيرات

### ١) الكلمة تنقطع نصفين (مثل «جاموس» → «جامو/س»)
**السبب:** في الجولة الثامنة أضفت `overflow-wrap: anywhere; word-break: break-word`
لمنع الزر الأصفر من تغطية الكلمة — لكنها سمحت للمتصفح بكسر الكلمة في أي حرف.

**الحل (سببي لا ترقيعي):**
- `word-break: keep-all; overflow-wrap: normal; hyphens: none` → الكلمة لا تنكسر أبداً في وسطها،
  والبطاقة ذات كلمتين تلتف عند المسافة فقط.
- أُلغي الحشو الأفقي `padding-right: 18px` الذي كان يضيّق النص؛ بدلاً منه
  `padding-top: 25px` (أطول من الزر) → عرض البطاقة كامل متاح للكلمة والزر متجنَّب رأسياً.
- تصغير تلقائي متدرج حسب أطول كلمة مفردة: `len-m` (7+)، `len-l` (9+)، `len-xl` (11+).

### ٢) زر «الإعدادات الافتراضية»
`resetSettings()` تمسح `cardScale`/`infoScale`/`clueScale` وتعيد الصوت للتفعيل
ثم تستدعي `applyScales()` فترجع المتغيرات إلى 1 والقوائم لحجمها الأصلي.

### ٣) زر الصوت داخل الإعدادات
نُقل من شريط اللعب إلى نافذة الإعدادات كزر واضح («🔊 الصوت مفعّل» / «🔇 الصوت مكتوم»)
مع `updateSoundBtnUI()` لمزامنة حالته عند الفتح وعند التبديل.

### ٤) المؤقت بلا مربع
أُزيلت الخلفية والإطار من `.turn-timer` ومن حالة `.timer-danger` مع بقاء الرقم في مكانه نفسه.

### ٥) إرجاع تأثيرات الحركة فقط
`fadeUp` (دخول الأدوار/الغرف/الرسائل)، `softPulse` (نبض ظل حول الدور النشط)،
`shimmer` (لمعة على الأزرار)، `floatSoft` (طفو المؤقت وقت الخطر)، وتفاعل لمس `scale(0.95)`.
**لا شيء منها يغيّر أحجام العناصر أو تخطيط الصفحات** — حركة وظلال وشفافية فقط، وبلا `@media`.

**الاختبارات:** 47/47 ناجحة (تشمل اختبار انحدار: بدء اللعبة، اللوحة، زر التأكيد، دقة الأفتار).

## الجولة العاشرة: إصلاح الفصل الخاطئ عند تبديل التبويب/التطبيق

### المشكلة
مجرد فتح صفحة أخرى في المتصفح أو التنقل لتطبيق آخر كان يُحسب «انقطاع اتصال»
فتُلغى الجولة على الجميع.

### السبب الجذري
الاعتماد الكامل على `onDisconnect().remove()` من Firebase:
1. متصفحات الجوال تُجمّد التبويب في الخلفية فينقطع WebSocket ويُنفّذ الخادم `onDisconnect`
   ويُحذف مقعد اللاعب — رغم أن الصفحة حيّة ولم يغلق شيئاً.
2. `checkPlayersIntegrity()` كانت تُلغي الجولة **فوراً** بمجرد أن ترى دوراً بلا لاعب:
   `if (playerId && !gameState.players[playerId]) { cancelGame(...) }` — بلا أي مهلة.
3. مستمع `players` كان يطرد اللاعب فوراً إذا اختفى مقعده، بلا محاولة استرجاع.

### الحل: نبضة حياة + استرجاع المقعد
- **نبضة كل 5 ثوانٍ** في عقدة منفصلة `game/heartbeats/{id}` (منفصلة عمداً عن `players`
  حتى لا تُعيد رسم واجهة الجميع كل 5 ثوانٍ).
- **لا يُعتبر اللاعب منقطعاً إلا إذا صمتت نبضته 22 ثانية** (`HEARTBEAT_DEAD_MS`)،
  والتجميد المؤقت للتبويب لا يوقف النبض فعلياً.
- **إلغاء الإلغاء الفوري:** أي مقعد شاغر يمر عبر مهلة العودة (30 ثانية) أولاً.
- **`ensureMySeatAlive()`**: إذا حُذف مقعدي بالخطأ وأنا متصل، يُعاد إنشاؤه مع دوري تلقائياً
  بدل طردي؛ والطرد لا يقع إلا بعد فشل الاسترجاع (مهلة 1.5 ثانية).
- **`visibilitychange`**: عند العودة للصفحة → نبضة فورية + فحص المقعد + محاولة استئناف الدور.
- **`pagehide`**: الإغلاق الحقيقي للصفحة يُزيل اللاعب فوراً بلا انتظار،
  مع استثناء `persisted` (ذاكرة bfcache) حتى لا يُطرد من عاد للصفحة.
- **حارس `isLeavingPage`** يمنع تعارضاً اكتشفه الاختبار: كان الاسترجاع يُعيد إنشاء
  المقعد بعد إغلاق الصفحة مباشرة.

### النتيجة
| الحالة | قبل | بعد |
|---|---|---|
| تبديل تبويب/تطبيق دقيقة | تُلغى الجولة | تستمر بلا إزعاج |
| `onDisconnect` خاطئ | طرد فوري | استرجاع تلقائي |
| قفل الصفحة/المتصفح | يُحسب | يُحسب فوراً |
| انقطاع نت حقيقي | يُحسب | يُحسب بعد 22ث + مهلة عودة 30ث |

**الاختبارات:** 53/53 (24 للفصل + 29 انحدار).

## الجولة الحادية عشرة: نبض البطاقة + الطرد + العد التنازلي الكبير

### ١) بطاقة واحدة تنبض بدل اللوحة كلها
**السبب:** `renderBoard()` تُعيد بناء البطاقات الـ25 عبر `innerHTML` عند كل تحديث للوحة،
فتُعاد حركة `cardRevealEpic` على **كل** بطاقة مكشوفة سابقاً في كل مرة.

**الحل:** ذاكرة `animatedCards` تتذكّر البطاقات التي ظهرت حركتها؛ البطاقة المكشوفة حديثاً
فقط تأخذ `revealed`، والقديمة تأخذ `revealed no-reveal-anim` التي تُلغي الحركة
وتُثبّت الشكل النهائي. تُصفَّر الذاكرة عند لوحة جديدة.

### ٢) الحجم الافتراضي للبطاقات = 111%
غُيّر في `applyScales()` وفي قيمة المنزلق وعرضه، فيشمل ذلك زر «الإعدادات الافتراضية».

### ٣) زر الطرد لمدير الغرفة (صاحب التاج)
- زر «🚫 طرد» في شاشة اختيار الأدوار، يظهر لصاحب التاج فقط وخارج اللعب.
- نافذة تعرض الموجودين (عدا نفسه) مع زر طرد لكل لاعب.
- الطرد: تسجيل حظر في `game/bans/{id}` + إشعار في `game/kicked/{id}`،
  ثم إزالة الدور والنبضة والمقعد.
- المطرود يخرج للغرف مع رسالة، ويُسجَّل الحظر محلياً (`cn_bans`) فيُمنع من الدخول
  دقيقة كاملة عبر حارس في `attemptJoinRoom`، مع عرض الثواني المتبقية.

### ٤) «التخلي عن المنصب» → «التخلي عن الدور»

### ٥) عد تنازلي كبير في وسط الشاشة + انتقال انسيابي
- طبقة `#countdownOverlay` بأرقام ضخمة وحلقة تتوسّع ونصّ «استعدوا... اللعبة تبدأ»،
  وعند الصفر «يلا نبدأ!» بالأخضر.
- **الأداء:** كل الحركات على `transform`/`opacity` فقط (تعمل على كرت الشاشة)
  مع `will-change`، وبلا أي خاصية تُجبر إعادة التخطيط — فلا يتعلق الموقع.
- الانتقال لشاشة اللعب عبر `gameEnter` (0.4s) ويُشغَّل **مرة واحدة** عند الدخول الفعلي فقط.
- العدّاد الصغير القديم أُخفي، وأُضيف إخفاء الطبقة في المواضع الخمسة التي كانت تخفيه.

**الاختبارات:** 85/85 (49 للجولة + 36 انحدار).
اكتشف الاختبار خطأً حقيقياً: زر الطرد كان يبقى ظاهراً أثناء اللعب لأن مستمعي
`players`/`roles` لا يُعاد إطلاقهما عند بدء اللعبة — أُصلح في مستمع الحالة.

## الجولة الثانية عشرة: تصنيف الغرف + شاشات ثابتة بلا تمرير

### ١) شاشة الغرف: ثلاثة أزرار تصنيف
بدل عرض كل الغرف دفعة واحدة، صارت الشاشة تعرض ثلاثة أزرار:
«غرف 2v2» (4 لاعبين)، «غرف 3v3» (6)، «غرف 4v4» (8).
- `selectedRoomMode` + `showRoomsOfMode(mode)` + `backToModes()`.
- `renderRoomsList()` تتخطى أي غرفة لا يطابق `room.mode` النوع المختار،
  وتعرض رسالة واضحة إن لم توجد غرف من النوع.
- العنوان يتغيّر حسب النوع، وزر «رجوع لأنواع الغرف» يعيد للاختيار.
- الرجوع للوبي (دخول/خروج من غرفة) يعيد دائماً لشاشة الأنواع عبر `backToModes()`.
- بعد إنشاء غرفة يُفتح تلقائياً تصنيفها.

### ٢) شاشات ثابتة: كل شيء ظاهر بلا تمرير
- `body { overflow: hidden }` + كل شاشة `height: 100vh/100dvh` وعمود مرن
  (`100dvh` مهم للجوال لأنه يستثني شريط المتصفح المتغيّر).
- **الشاشات الأربع** حُوِّلت إلى `flex` (كان `rolesScreen` يُعرض بـ`block`
  في 6 مواضع و`roomsScreen` في موضعين — وهذا كان سيُبطل التخطيط الجديد).
- **لوحة البطاقات**: كانت `height: 52vh` ثابتة قد تفيض؛ صارت `flex: 1 1 auto`
  فتأخذ المتبقي تلقائياً — والنتيجة بطاقات **أكبر** لا أصغر
  (71px بدل ~55px على شاشة 640).
- المناطق التي قد تطول (قائمة المتواجدين، قائمة الغرف، حاوية الفرق)
  تمرّر **داخلياً** بدل تمرير الصفحة كلها.
- تقليل الهوامش والعناوين في شاشة الأدوار، والنوافذ المنبثقة
  `max-height: 92dvh` فلا تتجاوز الشاشة.

**التحقق الحسابي:** شاشة الأدوار تستهلك 286px من 640px (المتبقي 354px لصف
أدوار يحتاج 117px)، وشاشة الغرف 383px من 640px.

**الاختبارات:** 81/81 (42 للجولة + 39 انحدار).

---

## الجولة 13 — إصلاح بقاء اللاعب في الغرفة بعد الضغط على «خروج»

### البلاغ
عند ضغط اللاعب زر «خروج» من شاشة الأدوار، كان الخروج ينفَّذ عنده فقط: يخرج من جهته
لكنه يبقى داخل الغرفة ويستمر ظهوره في قائمة «المتواجدين» عند بقية اللاعبين.

### السبب الجذري
انحدار (regression) نتج عن آلية «استعادة المقعد» المضافة في الجولة 10:

1. `exitRoom()` يحذف `PLAYERS_REF/{myPlayerId}` لكنه **لا يرفع أي راية** تُعلن أن الخروج مقصود
   (راية `isLeavingPage` تُرفع فقط في حدث `pagehide`).
2. مستمع اللاعبين يرى مقعدي اختفى بينما `CURRENT_ROOM_ID` و`myPlayerId` ما زالا مضبوطين،
   فيظن أن `onDisconnect` انطلق بالخطأ (تبديل تبويب مثلاً) ويستدعي `ensureMySeatAlive()`.
3. `ensureMySeatAlive()` كان يخرج فقط عند
   `isLeavingPage || isSiteOwner || !myPlayerId || !PLAYERS_REF || !CURRENT_ROOM_ID` — ولا شيء منها
   محقق في تلك اللحظة، فيُعيد كتابة المقعد في Firebase.
4. محلياً كان `exitRoomFromKick()` ينقل الخارج إلى شاشة الغرف، فيظن أنه خرج
   بينما المقعد المُستعاد يظل معروضاً للجميع.

### الإصلاح
استُخدم المتغير `isLeavingRoom` (كان معرَّفاً وغير مستعمل) كراية «خروج مقصود»:

| الموضع | التعديل |
|---|---|
| `exitRoom()` | `isLeavingRoom = true;` كأول سطر، قبل أي حذف ولكلا المسارين (لاعب/مالك مشاهد) |
| `ensureMySeatAlive()` | الخروج المبكر صار `if (isLeavingPage \|\| isLeavingRoom) return;` |
| مستمع اللاعبين | شرط الاستعادة صار `!isLeavingPage && !isLeavingRoom && !seatRecoveryTried && ...` |
| `attemptJoinRoom()` | `isLeavingRoom = false;` و`kickedOut = false;` عند كل دخول جديد (بعد كل الفحوص المانعة) حتى تعود الحماية للعمل |

تحصين إضافي في `exitRoom()`: كان `onDisconnect().cancel()` يُنفَّذ **قبل** `remove()`،
فلو مات الاتصال في تلك اللحظة يبقى المقعد للأبد بلا أي جهة تحذفه. صار الترتيب
`remove().then(() => onDisconnect().cancel())` ليبقى الخادم شبكة أمان حتى ينجح الحذف فعلياً.

### الاختبارات — 76/76
- `t_r13.js` (29): إزالة الخارج عند الجميع، تحرير دوره، إعادة الدخول وعودة الحماية،
  تنظيف الغرفة عند خروج آخر لاعب، الخروج أثناء اللعب، بقاء استعادة المقعد للانقطاع الوهمي،
  وعدم الاستعادة بعد إغلاق الصفحة فعلاً.
- `t_r13b.js` (14): الطرد، الخروج المتتابع لستة لاعبين، خروج المالك، خروج المالك المشاهد.
- `t_all.js` (33): انحدار شامل للجولات السابقة (الثوابت، الأمان، تخطيط 12، الجولة 11، الجولة 9،
  سير لعبة كامل، الدردشة العامة، سعة الغرفة).

---

## الجولة 14 — اختبار شامل للموقع بالكامل

اختبار بمعنى اللعب الفعلي: عملاء متعددون في وقت واحد عبر كل الأطوار، مع تسجيل
كل خطأ تشغيل وكل تنبيه يظهر للاعب.

### نطاق الاختبار — 8 جلسات / 268 فحصاً
| الجلسة | المحتوى | النتيجة |
|---|---|---|
| 1 | الأطوار 2v2 و3v3 و4v4 تعمل في نفس اللحظة، سلامة اللوح وعزل الغرف | 34/34 |
| 2 | مباريات كاملة: تلميح، تخمين صحيح/خاطئ/محايد، القنبلة، الفوز، منع اللعب خارج الدور | 29/29 |
| 3 | الفوضى: انقطاع، خروج أثناء اللعب، المؤقت، الشات العام والخاص، الطرد والحظر | 31/31 |
| 4 | الشاذ: تبديل التبويب، الضغط المتكرر، XSS، الغرفة الممتلئة، المالك المشاهد، التخلي عن الدور | 33/33 |
| 5 | الضغط: 58 لاعباً في 10 غرف متزامنة، التنقل بين الشاشات، الإعدادات، 30 دخول/خروج متتالٍ | 58/58 |
| 6 | السباقات: تسابق على نفس الدور، خروج متزامن، انقطاع مزدوج، الاستسلام، مباراتان متتاليتان | 25/25 |
| 7 | انحدار التاج (الجولة 14) | 25/25 |
| 8 | انحدار الجولات 1–13 | 33/33 |

### 🐞 خلل حقيقي مكتشف ومُصلح: الغرفة تبقى بلا صاحب

**الأعراض:** عند خروج مدير الغرفة أو انقطاعه، لا ينتقل التاج لأحد ⇒ تبقى الغرفة
بلا مدير نهائياً (`عدد المدراء = 0`).

**السبب:** منطق منح التاج موجود داخل `attemptJoinRoom` فقط (يُفحص عند **الدخول**)،
فلا شيء يعيد الفحص عند **المغادرة**.

**الأثر الخطير:** إلغاء الجولة بعد انتهاء مهلة إعادة الاتصال مشروط بـ
`currentPlayer.isOwner`. فإذا انقطع المدير نفسه أثناء اللعب لا يبقى من يلغي الجولة:

```
قبل الإصلاح: بعد 110 ثانية → شاشة اللعب ما زالت ظاهرة، شريط إعادة الاتصال عالق،
              بيانات اللوح موجودة  ⇒ الجولة معلّقة للأبد
```

كما تختفي كل صلاحيات المدير (زر الطرد، الفرق العشوائية) عن الجميع.

**الإصلاح:** دالة `ensureRoomHasOwner()` تُستدعى من مستمع اللاعبين عند كل تغيّر
في القائمة. إذا لم يوجد أي `isOwner === true` يتولى **أقدم لاعب** (أصغر مفتاح بعد
الفرز) القيادة. المرشح يُحسب بنفس الطريقة عند كل العملاء وكل عميل يرشّح نفسه فقط
(`heir !== myPlayerId → return`)، فلا يقع تسابق ولا يظهر تاجان. المالك المشاهد
مستثنى لأنه لا يملك `myPlayerId` أصلاً.

```
بعد الإصلاح: بعد 50 ثانية → rolesScreen، بيانات اللعبة مُسحت، مدراء = 1 ✔
```

### حالات فُحصت وثبت أنها سليمة (لا تحتاج تعديل)
- الضغط المتكرر على بطاقة مكشوفة: يمنعه صنف `revealed` على الكارت.
- مخمّنان يكشفان بطاقتين معاً: النتيجة تبقى متسقة (9 → 7).
- بقايا الجولة السابقة: `winner` و`clue` تُصفَّر إلى قيم فارغة ولا تظهر في الواجهة.
- التلميح الخبيث والاسم الخبيث: مُعقّمان بالكامل، و`safeImgSrc` يرفض `javascript:` و`http:`.
- التلميح الطويل يُقصّ إلى 40 حرفاً، والغرف الممتلئة ترفض الزائد برسالة واضحة.

---

## الجولة 15 — بيئة اختبار أقوى: DOM حقيقي بدل محاكي يدوي

### لماذا؟
الاختبارات السابقة كانت تعمل على **محاكي DOM مكتوب باليد**. هذا سبّب نتائج مضلّلة:
فشلان وهميان (الضغط المتكرر على بطاقة، وترتيب عناصر الشاشة) كانا بسبب نقص المحاكي
لا بسبب التطبيق، واستهلكا وقتاً في التحقيق.

### ما تغيّر
اعتُمد **jsdom**: محرك DOM حقيقي يحلّل ملف `كود نيمز.html` نفسه.

| القدرة | المحاكي القديم | مختبر jsdom |
|---|---|---|
| مصدر الصفحة | عناصر تُخترع عند الطلب | الملف الحقيقي (204 عنصراً) |
| أصناف CSS | لا تُطبَّق | `classList` حقيقي + `getComputedStyle` |
| النقر | استدعاء دوال مباشرة | `dispatchEvent` يمر عبر `onclick` |
| الحقن (XSS) | فحص نصي تقريبي | محلل HTML حقيقي يكشف تنفيذ الكود فعلاً |
| العناصر الناقصة | تُخترع بصمت | تُكتشف فوراً كخطأ |

تُشغَّل الصفحة بـ`runScripts:'dangerously'` بعد نزع سكربت التطبيق من الـHTML وحقنه
مرة واحدة (وإلا نُفِّذ مرتين). أُضيف polyfill لـ`innerText` لأن jsdom لا يدعمه —
قيد معروف في jsdom لا خلل في التطبيق.

### النتائج — 159 فحصاً، صفر أخطاء تشغيل
| المختبر | المحتوى | النتيجة |
|---|---|---|
| 1 | مباراة 2v2 كاملة بالنقر فقط: دخول، اختيار طور، تلميح، تخمين، فوز | 22/22 |
| 2 | فحوص CSS محسوبة: شاشة واحدة ظاهرة، لا تمرير، flex، الترتيب، حجم 111 | 28/28 |
| 3 | أمان: 6 حمولات XSS عبر الاسم والشات والتلميح واسم الغرفة | 30/30 |
| 4 | الحرِج: الخروج، انتقال التاج، انقطاع المدير، تبديل التبويب، الطرد، الشات | 34/34 |
| 5 | الحِمل: 58 لاعباً في 10 غرف متزامنة + 40 دورة دخول/خروج | 45/45 |

### تأكيدات مهمة
- **إصلاح الجولة 13** (الخروج) و**إصلاح الجولة 14** (التاج) تأكّدا بالنقر الحقيقي على الأزرار.
- **الضغط المتكرر على بطاقة مكشوفة**: ثبت الآن بشكل قاطع أن صنف `revealed` يمنعه
  (6 نقرات لم تغيّر النقاط) — وهو ما عجز المحاكي القديم عن إثباته.
- **كل حمولات XSS محجوبة**: التلميح الخبيث يُعرض كنص حرفي ولا يُنشئ أي عنصر خطر.
- **الكاتب يرى الألوان والمخمّن لا يراها** — تحقّق عبر أصناف CSS الفعلية.
- `mode-2v2` على حاويات الفرق بقايا ميتة في الـHTML بلا أي قاعدة CSS تستهدفها
  (صناديق الأدوار تُبنى ديناميكياً 2/3/4 حسب الطور) — غير ضار، تُرك كما هو.

> بقي قيد واحد: لا يمكن تنزيل متصفح حقيقي في هذه البيئة (محاولة Playwright تفشل
> بانقطاع التحميل)، لذا تبقى القياسات البصرية الدقيقة (بكسل/خطوط) خارج التغطية.

---

## الجولة 16 — تغيير الاسم إلى NJD Code Names وإعادة تصميم اللوحة اللونية

### 1) الاسم
- `<title>` صار `NJD Code Names` (كان «لعبة كود نيمز - النسخة الاحترافية»).
- شعار جديد في شاشة البداية: `NJD` بخط ضخم متدرّج فضّي + سطر `CODE NAMES` بتباعد أحرف.
- شعار مصغّر في أعلى شاشة الغرف.

### 2) اللوحة: رمادي وأسود مع فضّي بلاتيني
كان الموقع ذهبياً بالكامل (نحو 40 موضعاً) مع ألوان دخيلة (أخضر، بنفسجي، سماوي).
اللون المميّز الجديد هو **الفضّي البلاتيني** لأنه ينسجم طبيعياً مع الرمادي.

بُنيت لوحة كاملة في `:root` بدل الأرقام المبعثرة:

| المجموعة | المتغيّرات |
|---|---|
| أسطح متدرّجة | `--void #0a0b0c` · `--bg-dark #121417` · `--surface-1 #1a1d21` · `--surface-2 #22262b` · `--surface-3 #2b3036` · `--surface-4 #343a41` |
| حدود | `--line #3a4048` · `--line-soft` · `--line-strong #525a63` |
| الفضّي المميّز | `--silver-br #e8ecf1` · `--silver #c8ced6` · `--silver-dim #8b939c` · `--silver-deep #6b7480` |
| النصوص | `--text #e6e9ed` · `--muted #9aa2ab` |
| الأحمر الوظيفي | `--danger #a34a4a` · `--danger-br #cf6a6a` · `--danger-dark #6e2626` |

**عدد الاستبدالات:** 146 في CSS + 21 في HTML + 13 في JS. لم يبقَ أي لون قديم
(`#D4AF37`, `#ffd700`, `#32CD32`, `#ce93d8` …) — فقط الأبيض والأسود الصريحان.

### 3) ألوان الفرق: خُفِّض تشبّعها ولم تُلغَ
الأحمر والأزرق **وظيفيان** (يحدّدان الفريق) فلا يجوز جعلهما رماديين:

| | قبل | بعد | التشبّع |
|---|---|---|---|
| الأحمر | `#d32f2f` | `#b64444` | 65% → 46% |
| الأزرق | `#0288d1` | `#43759c` | 98% → 39% |

المسافة اللونية بين الفريقين = **153** (فوق 100 يعني تمييزاً ممتازاً)، فالانسجام
مع الرمادي تحقّق دون فقدان المعنى. خلفية شاشة اللعب تحوّلت من متدرّج أحمر
(`#2c1010`) إلى رمادي داكن (`#1b1f24`).

### 4) الأزرار
- أزرار الأطوار الثلاثة: بدل الأزرق/الأخضر الدخيلين، لكل طور **شريط جانبي فضّي**
  بدرجة إضاءة مختلفة (`silver-deep` → `silver` → `silver-br`) = تمييز بصري ضمن اللوحة.
- الأسطح صارت متدرّجات `linear-gradient(160deg, surface-2, surface-1)` بحدّ `1px` نظيف.

### 5) التحقق من التباين (WCAG)
فُحصت كل التركيبات حسابياً:
- الفضّي/النص على كل الأسطح: **8.4 – 15.5** (AAA).
- أبيض على الأحمر **5.39**، على الأزرق **4.93**، على الأسود **14.61** — كلها فوق AA.
- نص داكن على المحايد **9.03**.
- **خلل أُصلح:** تدرّج `.enter-btn` كان ينتهي بـ`silver-deep` فيهبط التباين إلى
  **3.99** (تحت AA). غُيّر الطرف إلى `silver` فصار **11.92**. ونفس الإصلاح لـ`.owner-tag`.

### 6) الاختبار — 49 فحصاً
- `theme.js` (19): المتغيّرات، الاسم، الشعار، غياب الألوان القديمة، تمايز الفرق.
- `verify.js` (30): مباراة كاملة بالنقر حتى الفوز، الأطوار الثلاثة، الخروج وانتقال
  التاج، الشعار في الواجهة، أمان الشات — **صفر أخطاء تشغيل**.
