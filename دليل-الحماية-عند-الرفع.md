# دليل الحماية عند رفع الموقع على استضافة

> سؤالك: كيف أخفي الكود عن `view-source:`؟
> الجواب الصادق: **مستحيل تقنياً** — لكن هذا ليس خطرك الحقيقي.

---

## الجزء الأول: لماذا لا يمكن إخفاء الكود

المتصفح **يجب** أن ينزّل الكود كاملاً لكي يشغّله. لا يوجد استثناء:

| الحيلة | لماذا تفشل |
|---|---|
| تعطيل الزر الأيمن | `Ctrl+U` و`F12` وقائمة المتصفح تتجاوزها |
| كشف DevTools وإعادة التوجيه | تُعطَّل من `Settings → Disable JavaScript` |
| حذف وسم `<script>` بعد التشغيل | الكود ظاهر في تبويب Sources، ووصل الجهاز أصلاً |
| `eval` وتحميل ديناميكي | يظهر تحت Sources → Eval Scripts، والملف في Network |
| التشفير | مفتاح فكّ التشفير لازم يكون في الصفحة نفسها! |

**والأبسط من كل ذلك:** `curl https://موقعك.com` يجلب الملف كاملاً بلا
متصفح أصلاً. لا JavaScript يعمل، ولا حيلة تنفع.

> لو كان إخفاء الكود ممكناً، لما استطاع أحد قراءة كود فيسبوك أو
> يوتيوب — وكلها مقروءة بالكامل.

**كل من يبيعك «حماية كود جافاسكربت» يبيعك وهماً.**

---

## الجزء الثاني: خطرك الحقيقي (اقرأ هذا)

فحصتُ ملفك. المكشوف فعلاً:

```js
apiKey: "AIzaSyCEkgPjbZlW9Jhc9XKi0cN2wY_ZFrAqLOA"
databaseURL: "https://code-names-c41b2-default-rtdb.europe-west1..."
```

### مفتاح Firebase: مكشوف بالتصميم ✅

هذا **ليس** كلمة سر. جوجل صمّمته ليكون علنياً — هو مجرد «عنوان» يقول
للمتصفح أي مشروع يخاطب. كل تطبيقات Firebase في العالم تكشفه.

**لا تحاول إخفاءه ولا تغيّره.** لن يفيد شيئاً.

### الخطر الفعلي: قواعد قاعدة البيانات ⚠️

الحماية الحقيقية تجري على **خوادم جوجل**، لا في المتصفح. لو كانت
قواعدك في «وضع الاختبار» فأي شخص يملك رابط قاعدتك — وهو مكشوف —
يستطيع **مسح كل بياناتك بأمر واحد**، بلا حاجة لقراءة كودك إطلاقاً.

**افحص الآن:** [console.firebase.google.com](https://console.firebase.google.com)
← مشروعك ← Realtime Database ← تبويب **Rules**

**إذا رأيت هذا فأنت مكشوف تماماً:**

```json
{ "rules": { ".read": true, ".write": true } }
```

---

## الجزء الثالث: القواعد الصحيحة لموقعك

فحصتُ مسارات قاعدتك: `/rooms` و`/winsHistory` فقط. الصق هذا مكان
قواعدك الحالية واضغط **Publish**:

```json
{
  "rules": {
    ".read": false,
    ".write": false,

    "rooms": {
      ".read": true,
      ".write": true,
      "$roomId": {
        "name":       { ".validate": "newData.isString() && newData.val().length <= 30" },
        "maxPlayers": { ".validate": "newData.isNumber() && newData.val() >= 4 && newData.val() <= 8" },
        "mode":       { ".validate": "newData.isString() && newData.val().matches(/^(2v2|3v3|4v4)$/)" },
        "createdAt":  { ".validate": "newData.isNumber()" },
        "temporary":  { ".validate": "newData.isBoolean()" },

        "customWords": {
          "$i": { ".validate": "newData.isString() && newData.val().length <= 20" }
        },

        "game": {
          ".read": true,
          ".write": true,
          "players": {
            "$pid": {
              "name": { ".validate": "newData.isString() && newData.val().length <= 20" }
            }
          }
        },

        "globalChat": {
          ".read": true,
          ".write": true,
          "$msgId": {
            "text": { ".validate": "newData.isString() && newData.val().length <= 300" },
            "name": { ".validate": "newData.isString() && newData.val().length <= 20" }
          }
        }
      }
    },

    "winsHistory": {
      ".read": true,
      ".write": true,
      "$winId": {
        ".validate": "newData.hasChildren(['ts'])",
        "ts": { ".validate": "newData.isNumber()" }
      }
    }
  }
}
```

### ماذا تفعل هذه القواعد

| القاعدة | الفائدة |
|---|---|
| `".read": false` في الجذر | لا أحد يسحب قاعدتك كلها بطلب واحد |
| حصر الكتابة في `rooms` و`winsHistory` | لا يستطيع أحد إنشاء فروع غريبة في قاعدتك |
| `.validate` على أطوال النصوص | لا أحد يحشو نصاً بحجم ميجابايت فيفجّر حصتك المجانية |
| `maxPlayers` بين ٤ و٨ | لا غرفة بألف مقعد تُثقل اللعبة |
| `mode` بنمط محدد | لا قيم عشوائية تكسر شاشة الغرف |

> **اختبرتُ هذه القواعد على مسارات كودك الخمسة قبل تسليمها**، وصحّحتُ
> فيها خطأين كنتُ سأسلّمهما لك:
> ١) كانت تمنع إنشاء الغرف الافتراضية، لأن الكود يكتب على `/rooms`
> نفسه عبر `ROOMS_REF.set(defaults)` لا على غرفة مفردة.
> ٢) كانت تمنع إنشاء غرفة بلا كلمات مخصّصة، لأن Firebase يعتبر المصفوفة
> الفارغة `[]` قيمةً معدومة فيسقط شرط `.validate` عليها.

> **ملاحظة صادقة:** هذه القواعد تمنع **العبث والإفساد**، لكنها تُبقي
> اللعب مفتوحاً للجميع (`.write: true` داخل الغرف) لأن لعبتك بلا
> تسجيل دخول. لإغلاق أقوى تحتاج Firebase Anonymous Auth — وهذا تغيير
> أكبر أستطيع تنفيذه لك لو أردت.

---

## الجزء الرابع: خطوتان إضافيتان (١٠ دقائق)

### ١) قيّد المفتاح بنطاقك

[console.cloud.google.com](https://console.cloud.google.com) ←
APIs & Services ← Credentials ← اختر مفتاحك:

- **Application restrictions** → HTTP referrers
- أضف: `موقعك.com/*` و `*.موقعك.com/*`

هذا يمنع غيرك من استعمال حصتك في موقعه.

### ٢) فعّل App Check

Firebase Console ← App Check ← reCAPTCHA v3. يمنع الطلبات التي لا
تأتي من موقعك الحقيقي. **هذه أقوى حماية عملية متاحة لك.**

---

## الجزء الخامس: كلمة سر المالك

```js
const OWNER_PASS_HASH = 'f8f6c74924d209f82aaa8fee410ea3374e8e556a405f098f5174525d4cac420a';
```

**الوضع الحالي — نصف جيد:**
- ✅ الكلمة نفسها ليست مكتوبة، بل بصمتها فقط (SHA-256).
- ❌ لكنها قصيرة جداً. **كسرتُها فعلياً في أقل من ثانية** بتجربة كل
  تركيبات ١–٣ أحرف — ١٨٬٢٧٨ احتمالاً فقط، وهذا لا شيء لجهاز حديث:

  ```
  كسرتُ كلمة السر: SMO
  ```

  الملح `codenames-owner-v1:` ثابت ومكتوب في الكود، فلا يزيد الصعوبة
  شيئاً — الملح يفيد ضد الجداول الجاهزة فقط، لا ضد من يقرأ كودك.
- ⚠️ والأهم: التحقق يجري **في المتصفح**. من يقرأ كودك يستطيع تنفيذ
  `isSiteOwner = true` في الـConsole ويصير مالكاً بلا كلمة سر أصلاً.

**ما الذي يخسره؟** صلاحيات المالك عندك: حذف الغرف وطرد اللاعبين. ليست
كارثة مالية، لكنها إزعاج حقيقي.

**العلاج الحقيقي الوحيد:** نقل التحقق للخادم (Cloud Function) بحيث
تُمنح صلاحية المالك من جوجل لا من المتصفح.

**علاج فوري مجاني:** غيّر كلمة السر إلى شيء طويل وغير متوقّع
(١٦ حرفاً فأكثر). أستطيع توليد البصمة الجديدة لك في ثانية — قل الكلمة
وأنفّذها.

---

## الخلاصة

| ما طلبته | الواقع |
|---|---|
| إخفاء الكود عن view-source | ❌ مستحيل — لا تضيّع وقتك |
| حماية بياناتك من العبث | ✅ ممكنة تماماً — بقواعد قاعدة البيانات |

### افعل بالترتيب

1. **الآن:** افحص قواعد Firebase. لو كانت `true/true` فأنت مكشوف.
2. **الآن:** الصق القواعد أعلاه واضغط Publish.
3. **اليوم:** قيّد المفتاح بنطاقك + فعّل App Check.
4. **متى ما أردت:** كلمة سر مالك أقوى، أو نقلها للخادم.

**ولا تفعل:** لا تغيّر مفتاح Firebase، ولا تعطّل الزر الأيمن، ولا
تشتري «حماية جافاسكربت».

> كودك سيُقرأ. بياناتك لا يجب أن تُمسّ. الثاني هو المهم — وهو تحت
> سيطرتك بالكامل.


---

## ملحق (الجولة ٥٣): إعادة تحقّق من القواعد

مرّت ٨ جولات تعديل على الكود منذ كتابة هذا الدليل (الجولات ٤٢–٥٢:
ترتيب الغرف، الدخول الفوري، حارس الجلسة، المهندس، تعديل الكلمات).
أعدتُ فحص القواعد على الكود الحالي بمحاكي يطبّق منطق Firebase.

**النتيجة: ٢٨/٢٨ — القواعد ما زالت صحيحة ومطابقة تماماً.**

فُحص:
- **١١ مسار كتابة/قراءة حقيقياً** من الكود: `/rooms` (الغرف
  الافتراضية عبر `ROOMS_REF.set`)، إنشاء غرفة، `game`، `board`،
  `players`، `doubts`، `heartbeats`، `roomDeleted`، `globalChat`،
  `winsHistory`، وقراءة الغرف — **كلها تعمل**.
- **٤ محاولات عبث**: قراءة الجذر، الكتابة على الجذر، إنشاء فرع
  `hackedBranch`، إنشاء فرع `users` — **كلها مرفوضة**.
- **١٣ حالة تحقّق من البيانات**: اسم غرفة > ٣٠، نمط `9v9`،
  `maxPlayers=9999`، رسالة > ٣٠٠ حرف، اسم لاعب > ٢٠، كلمة مخصّصة
  > ٢٠، `ts` نصّي — **كلها مرفوضة**، والسليم منها مقبول.

**تأكيد خاص:** الغرف الافتراضية (`room_2v2_1`…) تُكتب بلا `createdAt`
ولا `temporary`، والقواعد تقبلها — لأن `.validate` لا يُطبَّق على
الحقول الغائبة. هذا كان أحد الخطأين المصحّحين أصلاً، وما زال سليماً.

**لا حاجة لأي تعديل على القواعد المكتوبة أعلاه.** انسخها كما هي.
